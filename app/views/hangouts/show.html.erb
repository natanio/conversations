	<div class="row">
		<div class="small-12 medium-12 large-12 top-margin">
			<ul class="breadcrumbs">
	 			<li><%= link_to @language.name + " Conversations", language_hangouts_path %></li>
	  			<li class="current"><a href="#"><%= @hangout.name %></a></li>
	  			<% if params[:tag].present? %>
	  			<li><%= link_to 'Remove Tags', language_hangout_path(@language, @hangout) %></li>
	  			<% end %>
			</ul>
		</div>
	</div>
  

<%= render partial: 'posts/index', locals: {language: @language, hangout: @hangout} %>